rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permitir acceso de lectura y escritura a cualquier usuario autenticado en la aplicación
      allow read, write: if request.auth != null;
    }
  }
}
